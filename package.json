{
  "name": "digital-certificate-verification",
  "version": "1.0.0",
  "description": "Cryptographically verified digital document and certificate verification platform",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "test": "npm test --prefix backend",
    "seed": "npm run seed --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "keywords": [
    "certificate-verification",
    "cryptography",
    "ecdsa",
    "sha256",
    "qr-code",
    "audit-trail"
  ],
  "author": "Vivek Solanki",
  "license": "MIT"
}
